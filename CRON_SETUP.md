# 📅 Автоматическая ротация позиций — Простая инструкция

## Что это делает?

Каждую ночь в 03:00 по Москве система автоматически меняет позиции всех пакетов продвижения.
Например, владелец с пакетом "Золото" сегодня на 5-м месте, завтра может быть на 18-м, послезавтра на 2-м.

---

## 🚀 Пошаговая инструкция для Render.com

### ШАГ 1: Создайте секретный ключ

Это пароль для защиты вашей функции.

**Способ А — в браузере (самый простой):**
1. Откройте: https://www.random.org/strings/
2. Настройте:
   - **Generate:** 1 string
   - **Length:** 40 characters
   - **Characters:** Alphanumeric
3. Нажмите **Get Strings**
4. Скопируйте результат (например: `a7b2c9d4e1f6g3h8i5j0k2m7n9p4q6r8s1t3u5`)

**Способ Б — в консоли браузера:**
1. Нажмите **F12** на клавиатуре
2. Перейдите на вкладку **Console**
3. Вставьте и нажмите Enter:
```javascript
Math.random().toString(36).substring(2) + Math.random().toString(36).substring(2) + Math.random().toString(36).substring(2)
```
4. Скопируйте строку, которая появится

**💾 СОХРАНИТЕ эту строку — она понадобится дальше!**

---

### ШАГ 2: Добавьте секрет в poehali.dev

1. Найдите форму **CRON_SECRET** выше в этом чате (должна быть видна)
2. Вставьте вашу строку из Шага 1
3. Нажмите кнопку **"Добавить"**

---

### ШАГ 3: Зарегистрируйтесь на Render.com

1. Откройте: **https://dashboard.render.com/register**
2. Выберите способ регистрации:
   - **GitHub** (рекомендуется — 1 клик)
   - Или введите **email и пароль**
3. Если через email — подтвердите почту (придёт письмо)
4. После входа вы увидите панель управления (Dashboard)

---

### ШАГ 4: Создайте Cron Job

1. В правом верхнем углу нажмите синюю кнопку **"New +"**
2. В выпадающем меню выберите **"Cron Job"**
3. Откроется форма создания задачи

---

### ШАГ 5: Заполните форму

Вы увидите несколько полей. Заполняйте строго по инструкции:

#### 1️⃣ Name (название задачи)
Введите:
```
daily-rotation
```

#### 2️⃣ Region (регион сервера)
Выберите из выпадающего списка:
```
Oregon (US West)
```
⚠️ Это единственный **бесплатный** регион!

#### 3️⃣ Command (команда для выполнения)
Скопируйте эту команду ЦЕЛИКОМ:
```bash
curl -X POST -H "Authorization: Bearer ВАШ_СЕКРЕТ" https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6
```

⚠️ **ВАЖНО:** Замените слова `ВАШ_СЕКРЕТ` на вашу строку из **Шага 1**!

**Пример готовой команды:**
```bash
curl -X POST -H "Authorization: Bearer a7b2c9d4e1f6g3h8i5j0k2m7n9p4q6r8s1t3u5" https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6
```

#### 4️⃣ Schedule (расписание запуска)
Введите строго эти символы:
```
0 0 * * *
```
Это означает: каждый день в 00:00 UTC (= 03:00 по Москве)

**Расшифровка:**
- `0` — минута (00)
- `0` — час (00 UTC = 03:00 МСК)
- `*` — каждый день месяца
- `*` — каждый месяц
- `*` — любой день недели

#### 5️⃣ Docker Command
Оставьте пустым (не заполняйте)

---

### ШАГ 6: Создайте задачу

1. Прокрутите вниз
2. Нажмите зелёную кнопку **"Create Cron Job"**
3. Подождите 10-30 секунд (Render создаёт задачу)
4. Вы попадёте на страницу вашего Cron Job

---

### ШАГ 7: Проверьте работу (тестовый запуск)

1. На странице Cron Job найдите кнопку **"Trigger Run"** (запустить вручную)
2. Нажмите её
3. Подождите 5-10 секунд
4. Посмотрите на результат:

✅ **Если видите "Success"** — всё работает отлично!

❌ **Если "Failed"** — проверьте:
   - Правильно ли вы вставили секрет в команду (Шаг 5.3)?
   - Добавили ли секрет в poehali.dev (Шаг 2)?

**Что вы должны увидеть при успехе:**
```json
{"success": true, "rotated": 0, "timestamp": "2026-01-27T12:34:56Z", "note": "..."}
```

---

## ✅ Готово! Что дальше?

### Автоматическая работа
Теперь **каждый день в 03:00 по Москве** Render будет автоматически вызывать вашу функцию, и позиции всех пакетов продвижения обновятся.

### Как посмотреть историю запусков
1. Откройте ваш Cron Job на Render
2. Перейдите на вкладку **"Logs"**
3. Там будут все запуски с датами и результатами

### Как проверить, что ротация работает
1. Зайдите в личный кабинет владельца на вашем сайте
2. Перейдите на вкладку **"Продвижение"**
3. Если есть активный пакет — вы увидите текущую позицию
4. На следующий день (после 03:00) позиция изменится

---

## ❓ Частые вопросы

**Q: Обязательно ли это настраивать?**  
A: Нет! Позиции обновляются автоматически при заходе на сайт. Но с cron-задачей обновление происходит строго в 03:00, что предсказуемее.

**Q: Это точно бесплатно?**  
A: Да, на Render.com Cron Jobs в регионе Oregon (US West) полностью бесплатные. Платить не нужно.

**Q: Что если я забуду настроить?**  
A: Ничего страшного — система продолжит работать, просто позиции обновятся при первом визите на сайт после полуночи.

**Q: Можно ли изменить время запуска?**  
A: Да! В поле Schedule измените первую цифру (час). Например:
   - `0 12 * * *` = каждый день в 15:00 МСК
   - `0 18 * * *` = каждый день в 21:00 МСК

**Q: Как отключить автозапуск?**  
A: Зайдите в Render → ваш Cron Job → справа вверху **Delete Service**.

---

## 🛠 Техническая информация

- **Функция:** `/backend/daily-rotation/`
- **URL:** https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6
- **Метод:** POST
- **Заголовок:** `Authorization: Bearer <CRON_SECRET>`
- **Успешный ответ:** `{"success": true, "rotated": 0, "timestamp": "...", "note": "..."}`

---

**Нужна помощь?** Напишите мне — разберём по шагам! 🚀
