# 📅 Настройка автоматической ротации позиций

## Что это?

Каждую ночь система автоматически меняет позиции всех активных пакетов продвижения. Владельцы с пакетом "Золото" могут оказаться на 1-м месте, на 15-м или на 30-м — каждый день новая позиция в пределах их диапазона.

---

## 🚀 Простой способ (без регистрации)

### Вариант 1: Render.com (бесплатно, навсегда)

1. **Зарегистрируйтесь:** https://dashboard.render.com/register
   - Можно через GitHub (1 клик)

2. **Создайте Cron Job:**
   - После входа нажмите **New +** → **Cron Job**
   
3. **Настройте:**
   - **Name:** `daily-rotation`
   - **Command:** 
   ```bash
   curl -X POST -H "Authorization: Bearer ВАШ_СЕКРЕТ" https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6
   ```
   ⚠️ Замените `ВАШ_СЕКРЕТ` на секрет из Шага 1 ниже!
   
   - **Schedule:** `0 0 * * *` (каждый день в 00:00 UTC = 03:00 МСК)
   - **Region:** Oregon (USA) — бесплатный регион
   
4. **Нажмите Create Cron Job**

### Вариант 2: GitHub Actions (бесплатно, если у вас есть GitHub)

Если ваш проект уже на GitHub:

1. **Создайте файл** `.github/workflows/daily-rotation.yml`:

```yaml
name: Daily Rotation

on:
  schedule:
    - cron: '0 0 * * *'  # Каждый день в 00:00 UTC (03:00 МСК)
  workflow_dispatch:  # Можно запустить вручную

jobs:
  rotate:
    runs-on: ubuntu-latest
    steps:
      - name: Call rotation endpoint
        run: |
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6
```

2. **Добавьте секрет в GitHub:**
   - Откройте Settings → Secrets and variables → Actions
   - Нажмите **New repository secret**
   - Name: `CRON_SECRET`
   - Value: ваш секрет (создайте в Шаге 1 ниже)
   - Save

3. **Готово!** GitHub будет автоматически запускать задачу каждый день.

---

## Шаг 1: Создайте секрет CRON_SECRET

Нужно создать случайный пароль для защиты функции.

**Простой способ:**
1. Откройте браузер (Chrome/Firefox/Safari)
2. Нажмите F12 → вкладка Console
3. Вставьте и нажмите Enter:
```javascript
Math.random().toString(36).substring(2) + Math.random().toString(36).substring(2) + Math.random().toString(36).substring(2)
```
4. Скопируйте результат (например: `k8m2n4p6q8r0s2t4u6v8w0x2`)

**Или используйте готовый:** `xr8k2m5p9t3w7z1d4g6j8n2q5s7v9y1b3c5e7`

5. В редакторе poehali.dev вставьте строку в форму **CRON_SECRET** (выше в чате)
6. Нажмите "Добавить"

---

## 🔧 Альтернатива: cPanel (если у вас хостинг)

Если у вас уже есть сайт на хостинге с cPanel:

1. Откройте **cPanel → Cron Jobs**
2. Добавьте новую задачу:
   - **Minute:** 0
   - **Hour:** 0
   - **Day:** *
   - **Month:** *
   - **Weekday:** *
   - **Command:**
   ```bash
   curl -X POST -H "Authorization: Bearer ВАШ_СЕКРЕТ" https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6
   ```

3. Сохраните

---

## ✅ Как проверить, что всё работает?

### Тест вручную (с компьютера)

Откройте терминал (Windows: Win+R → cmd, Mac: Terminal) и выполните:

```bash
curl -X POST -H "Authorization: Bearer ВАШ_СЕКРЕТ" https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6
```

**Ожидаемый ответ:**
```json
{"success": true, "rotated": 0, "timestamp": "2026-01-27T12:00:00+00:00", "note": "..."}
```

Если видите `{"error": "Unauthorized"}` — проверьте, что секрет правильный.

### Проверка в личном кабинете

1. Войдите как владелец с активным пакетом
2. Перейдите на вкладку **Продвижение**
3. Вы увидите текущую позицию (например: "Ваша позиция сегодня: 23")
4. На следующий день (после 03:00 МСК) позиция изменится

---

## ❓ Частые вопросы

**Q: Обязательно ли настраивать автоматический запуск?**  
A: Нет! Позиции пересчитываются автоматически при просмотре каталога. Автозапуск просто делает смену позиций предсказуемой (ровно в 03:00 МСК).

**Q: Что будет, если я ничего не настрою?**  
A: Система продолжит работать — позиции обновятся при первом заходе на сайт после полуночи.

**Q: Можно ли запускать несколько раз в день?**  
A: Да, но бессмысленно — функция выдаст ту же позицию до конца дня (так задумано).

**Q: Как отключить автозапуск?**  
A: Удалите задачу в том сервисе, где её создали (Render/GitHub Actions/cPanel).

---

## 🛠 Техническая информация

- **URL:** https://functions.poehali.dev/34172fa3-9c6c-45b1-ad46-7e7d37c6a6e6
- **Метод:** POST
- **Заголовок:** `Authorization: Bearer <CRON_SECRET>`
- **Ответ:** `{"success": true, "rotated": <количество>, "timestamp": "..."}`

---

**Рекомендация:** Используйте **Render.com** — самый простой и надежный вариант, регистрация через GitHub занимает 30 секунд.
