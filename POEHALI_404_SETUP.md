# Настройка корректного HTTP 404 для poehali.dev

## Текущая ситуация

Ваш сайт 120minut.ru развернут на платформе poehali.dev. Для корректной индексации Яндексом необходимо настроить правильную обработку несуществующих страниц (код 404).

## Что уже сделано ✅

1. **Создана красивая страница 404** (`public/404.html`)
   - Брендированный дизайн в стиле 120 МИНУТ
   - Правильные мета-теги для SEO
   - Ссылки на популярные разделы

2. **Настроены правила маршрутизации** (`public/_redirects`)
   - Список всех валидных маршрутов приложения
   - Обработка статических файлов
   - Fallback на index.html для SPA

3. **Конфигурация Nginx** (`public/nginx.conf`)
   - Готовая конфигурация для серверной обработки 404
   - Настройки кэширования и безопасности
   - Поддержка gzip сжатия

4. **Заголовки и кэширование** (`public/_headers`)
   - Оптимизация производительности
   - Правильные заголовки для SEO

## Как работает обработка 404 в SPA

**Проблема:** React SPA всегда возвращает код 200, даже для несуществующих страниц.

**Решение состоит из двух частей:**

### 1. Клиентская часть (уже работает)
- React Router показывает компонент `NotFound` для неизвестных маршрутов
- Пользователь видит красивую страницу 404
- **НО:** HTTP статус остаётся 200 ❌

### 2. Серверная часть (требуется настройка)
- Веб-сервер должен возвращать код 404 для несуществующих URL
- Поисковые боты получают правильный HTTP статус
- Яндекс корректно индексирует сайт ✅

## Варианты настройки для poehali.dev

### Вариант А: Если используется Nginx (рекомендуется)

1. **Разместите конфигурацию:**
   ```bash
   # Скопируйте готовый файл nginx.conf
   sudo cp public/nginx.conf /etc/nginx/sites-available/120minut.ru
   
   # Создайте симлинк
   sudo ln -s /etc/nginx/sites-available/120minut.ru /etc/nginx/sites-enabled/
   
   # Проверьте конфигурацию
   sudo nginx -t
   
   # Перезапустите Nginx
   sudo systemctl reload nginx
   ```

2. **Проверка:**
   ```bash
   # Существующая страница - должен вернуть 200
   curl -I https://120minut.ru/
   
   # Несуществующая страница - должен вернуть 404
   curl -I https://120minut.ru/nesushchestvuyushchaya-stranitsa
   ```

### Вариант Б: Если используется встроенный хостинг poehali.dev

Если у вас нет доступа к серверной конфигурации, используйте следующее решение:

1. **Создайте middleware функцию для обработки 404:**

Создайте файл `backend/handle-404/index.py`:

```python
import json
import os

def handler(event: dict, context) -> dict:
    '''Middleware для обработки 404 ошибок'''
    
    method = event.get('httpMethod', 'GET')
    path = event.get('path', '/')
    
    # Список валидных путей приложения
    valid_paths = [
        '/',
        '/city',
        '/listing',
        '/admin',
        '/owner',
        '/manager',
        '/om',
        '/um',
        '/accounting',
        '/career',
        '/sales-scripts',
        '/company-info',
        '/offer',
        '/add-listing'
    ]
    
    # Проверяем, начинается ли путь с валидного префикса
    is_valid = any(path == vp or path.startswith(vp + '/') for vp in valid_paths)
    
    if not is_valid:
        # Возвращаем 404
        with open('404.html', 'r', encoding='utf-8') as f:
            html_content = f.read()
        
        return {
            'statusCode': 404,
            'headers': {
                'Content-Type': 'text/html; charset=utf-8',
                'Cache-Control': 'no-cache'
            },
            'body': html_content,
            'isBase64Encoded': False
        }
    
    # Если путь валидный - отдаём index.html
    with open('index.html', 'r', encoding='utf-8') as f:
        html_content = f.read()
    
    return {
        'statusCode': 200,
        'headers': {
            'Content-Type': 'text/html; charset=utf-8',
            'Cache-Control': 'no-cache'
        },
        'body': html_content,
        'isBase64Encoded': False
    }
```

2. **Разверните функцию:**
   ```bash
   # Используйте инструменты poehali.dev для деплоя
   ```

### Вариант В: Cloudflare Pages (если используется)

Файлы уже настроены:
- `public/_redirects` - правила маршрутизации
- `public/_routes.json` - конфигурация Cloudflare
- `public/_headers` - заголовки

Cloudflare автоматически применит эти настройки после деплоя.

### Вариант Г: Netlify (если используется)

Netlify автоматически использует файл `public/_redirects`. После деплоя проверьте:

```bash
curl -I https://120minut.ru/test-404-page
# Должен вернуть: 200 (так работает Netlify для SPA)
```

**Важно:** Netlify по умолчанию возвращает 200 для всех маршрутов SPA. Для 404 нужна платная версия с Edge Functions.

## Проверка после настройки

### 1. Проверка через curl
```bash
# Главная страница - должен быть 200
curl -I https://120minut.ru/

# Несуществующая страница
curl -I https://120minut.ru/eto-nesushchestvuyushchaya-stranica-123

# Должен вернуть 404 (или 200 с перенаправлением на 404.html)
```

### 2. Проверка в браузере
1. Откройте https://120minut.ru/test-404-url
2. Нажмите F12 → вкладка Network
3. Проверьте статус первого запроса (должен быть 404)

### 3. Проверка в Яндекс.Вебмастер
1. Зайдите в Яндекс.Вебмастер → Инструменты → Проверка ответа сервера
2. Введите: `https://120minut.ru/test-404-page`
3. Должен показать статус: **404 Not Found**

## Время обновления индекса

После настройки:
- Яндекс проверит сайт в течение 1-3 дней
- Рекомендация в диагностике исчезнет автоматически
- Можно ускорить через "Переобход страниц"

## Нужна помощь?

Если не уверены, какой хостинг используется:

1. **Проверьте DNS:**
   ```bash
   dig 120minut.ru
   nslookup 120minut.ru
   ```

2. **Проверьте заголовки:**
   ```bash
   curl -I https://120minut.ru/ | grep -i server
   ```

3. **Свяжитесь с поддержкой poehali.dev** для уточнения типа хостинга

## Что дальше?

1. ✅ Файлы готовы - `404.html`, `nginx.conf`, `_redirects`
2. ⏳ Настройте серверную часть (выберите вариант выше)
3. ✅ Проверьте curl-запросами
4. ✅ Подождите 1-3 дня
5. ✅ Проверьте Яндекс.Вебмастер → Диагностика
6. ✅ Готово! 🚀

## Полезные ссылки

- [Яндекс.Вебмастер](https://webmaster.yandex.ru/)
- [Диагностика сайта](https://webmaster.yandex.ru/site/https:120minut.ru:443/diagnostics/)
- [Переобход страниц](https://webmaster.yandex.ru/site/https:120minut.ru:443/indexing/recrawl/)
