# 📞 Инструкция по настройке системы подмены номеров

## ✅ Что уже готово:

1. **База данных:**
   - Таблица `virtual_numbers` — для хранения пула номеров
   - Таблица `call_tracking` — для истории звонков
   - Поле `short_title` в объектах для IVR
   - 5 тестовых номеров добавлены для демо

2. **Backend функции:**
   - `get-virtual-number` — выдаёт номер из пула на 30 минут
   - `route-call` — маршрутизирует входящие звонки с АТС

3. **Frontend:**
   - Кнопка "Позвонить" на карточках объектов
   - Автоматическое получение виртуального номера

---

## 🚀 Что нужно сделать:

### Шаг 1: Купить виртуальные номера в АТС

Рекомендуем **Zadarma** (можно и другую АТС):

1. Зарегистрируйтесь на https://zadarma.com
2. Купите **15 виртуальных номеров** (стоимость ~500₽/номер в месяц)
3. Запишите все номера

### Шаг 2: Добавить реальные номера в базу данных

Выполните SQL запрос (замените номера на ваши реальные):

```sql
-- Удалите тестовые номера
DELETE FROM virtual_numbers WHERE phone LIKE '+7495%';

-- Добавьте ваши реальные номера
INSERT INTO virtual_numbers (phone) VALUES
('+74951112201'),
('+74951112202'),
('+74951112203'),
('+74951112204'),
('+74951112205'),
('+74951112206'),
('+74951112207'),
('+74951112208'),
('+74951112209'),
('+74951112210'),
('+74951112211'),
('+74951112212'),
('+74951112213'),
('+74951112214'),
('+74951112215');
```

### Шаг 3: Настроить webhook в АТС

В личном кабинете Zadarma:

1. **API → Webhooks → Добавить webhook**
2. **Событие:** "Входящий звонок" (incoming call)
3. **URL:** `https://functions.poehali.dev/118f6961-69ab-4912-bbec-0481012af402`
4. **Метод:** POST
5. Сохраните

### Шаг 4: Настроить IVR и Whisper в АТС

**Для каждого виртуального номера настройте:**

#### Вариант A: Прямая переадресация с whisper

1. **Сценарий входящего звонка:**
   - При входящем звонке → Отправить webhook на ваш сервер
   - Получить ответ от сервера (телефон владельца)
   - Проиграть whisper владельцу: "Звонок с сайта 120 минут..."
   - Соединить с владельцем

#### Вариант B: IVR с подтверждением (рекомендуем)

1. **Загрузите аудиофайлы:**
   - `welcome.mp3`: "Здравствуйте! Вы звоните по объекту [название]. Нажмите 1 для соединения"
   - `not_found.mp3`: "К сожалению, мы не смогли определить объект. Зайдите на сайт 120minut.ru"
   - `whisper.mp3`: "Звонок с сайта 120 минут по объекту номер [ID]"

2. **Сценарий:**
   ```
   Входящий звонок
   ↓
   Webhook → ваш сервер возвращает:
   - action: "ivr_confirm"
   - message: текст для клиента
   - if_pressed_1: {to: "номер владельца", whisper: "текст для владельца"}
   ↓
   Играть message клиенту
   ↓
   Если клиент нажал 1 → играть whisper владельцу → соединить
   ```

### Шаг 5: Протестировать систему

1. **Откройте карточку объекта на сайте**
2. **Нажмите "Позвонить"**
3. **Увидите виртуальный номер** (например, +7-495-111-22-01)
4. **Позвоните с телефона**
5. **Проверьте:**
   - Владелец слышит whisper?
   - Соединение работает?
   - Можете перезвонить через 3 часа на тот же номер?

---

## 📊 Как это работает:

### При открытии карточки объекта:
```
Клиент нажимает "Позвонить"
↓
Запрос к /get-virtual-number
↓
Система выдаёт свободный номер из пула
↓
Привязка номера к объекту на 30 минут
↓
Запись в базу: клиент → номер → объект
↓
Клиент видит виртуальный номер
```

### При входящем звонке:
```
Клиент звонит на виртуальный номер
↓
АТС отправляет webhook на /route-call
↓
Система ищет в истории: откуда звонят + куда звонят
↓
Находит объект в базе
↓
Возвращает АТС:
- Текст для клиента (IVR)
- Номер владельца
- Текст для владельца (Whisper)
↓
АТС проигрывает IVR → Whisper → Соединяет
```

---

## 💰 Стоимость:

- **15 номеров × 500₽** = **7500₽/месяц**
- Обслуживает **все 5000+ объектов**
- Каждый номер может обработать **~48 показов в день** (30 мин × 48 = 24 часа)

---

## 🔧 Управление номерами:

### Добавить новые номера (если 15 мало):
```sql
INSERT INTO virtual_numbers (phone) VALUES
('+74951112216'),
('+74951112217');
```

### Посмотреть статистику использования:
```sql
SELECT 
  vn.phone,
  COUNT(ct.id) as total_calls,
  COUNT(ct.called_at) as completed_calls,
  MAX(ct.shown_at) as last_used
FROM virtual_numbers vn
LEFT JOIN call_tracking ct ON ct.virtual_number = vn.phone
GROUP BY vn.phone
ORDER BY total_calls DESC;
```

### Освободить занятые номера вручную:
```sql
UPDATE virtual_numbers 
SET is_busy = FALSE, 
    assigned_listing_id = NULL,
    assigned_at = NULL,
    assigned_until = NULL
WHERE assigned_until < NOW();
```

---

## ❓ FAQ:

**Q: Что если все 15 номеров заняты?**  
A: Система вернёт ошибку 503. Клиент увидит реальный номер владельца. Решение: добавить больше номеров в пул.

**Q: Можно ли использовать один номер на все объекты?**  
A: Да, но тогда клиенту придётся вводить номер объекта вручную через IVR. Не так удобно.

**Q: Как долго хранится история звонков?**  
A: Бесконечно. Можно настроить автоочистку старых записей (>90 дней).

**Q: Что если клиент звонит через месяц?**  
A: Система всё равно найдёт запись в истории и соединит с нужным владельцем!

---

## 📞 Контакты для помощи:

Если возникли проблемы с настройкой АТС — напишите, помогу настроить webhook и IVR!
